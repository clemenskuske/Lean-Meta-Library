# Namespaces Correct Failure

This package is a negative import fixture for the `namespaces-correct.mjs` checker.

Expected rejection: `namespaces correct` reports that package or declaration namespaces do not match metadata.
