# Namespaces Correct Failure

This package is a negative import fixture for the `general/namespaces-correct.mjs` checker.

Expected rejection: `namespaces correct` reports that package or declaration namespaces do not match manifest.
