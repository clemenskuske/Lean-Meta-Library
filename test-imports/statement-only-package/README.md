# Statement Only

This package is an acceptance fixture for submissions that publish statements
without submitting proof targets.

Expected acceptance: `run-all.mjs` passes when `StatementSubmissions` is present
and `ProofSubmissions` is omitted.
