# Update Missing Statement

This package is a negative import fixture for the `general/submission-update-policy.mjs` checker.

Expected rejection: the matching existing submission has a public statement that
is absent from the updated manifest.
