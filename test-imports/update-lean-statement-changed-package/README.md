# Update Lean Statement Changed

This package is a negative import fixture for the `general/submission-update-policy.mjs` checker.

Expected rejection: the matching existing submission contains a public statement
with a stored `InlineLeanStatement`, and the updated manifest changes that Lean
statement.
