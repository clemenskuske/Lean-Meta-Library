# Update Compatible

This package is an acceptance fixture for the `general/submission-update-policy.mjs` checker.

Expected acceptance: the matching existing submission is present in
`submissions.jsonl`, and every old public statement is still present with the
same stored Lean statement.
