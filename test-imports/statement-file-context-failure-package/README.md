# Statement File Context Failure

This package is a negative import fixture for the `statements/file-context.mjs`
checker.

Expected rejection: the context check reports a forbidden eval command in the
statement file.
