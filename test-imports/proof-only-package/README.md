# Proof Only

This package is an acceptance fixture for submissions that include a proof
package without publishing statement entries.

Expected acceptance: `run-all.mjs` passes when `ProofSubmissions` is present,
`StatementSubmissions` is omitted, and there are no submitted proof targets.
